import { Button } from "@mui/material";

const Home = () => {
  return <div className="min-h-screen flex flex-col gap-8 items-center justify-center">
    <h1 className="text-5xl">Test</h1>
    <Button variant="contained">Test</Button>
  </div>
}

export default Home;